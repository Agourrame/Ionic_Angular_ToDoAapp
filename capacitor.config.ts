import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.app',
  appName: 'ionic_project_one',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
